name: prod-build-deploy

on:
  push:
    branches: [prod]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout prod
        uses: actions/checkout@v4
        with:
          ref: prod

      - name: Install pnpm
        run: |
          corepack enable
          corepack prepare pnpm@latest --activate

      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm run docs:build

      - name: Push dist → release/prod 根目录
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: release/prod
          folder: /
          target-folder: .
          single-commit: true
          clean: true